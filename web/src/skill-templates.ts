export type SkillTemplate = {
  id: string;
  label: string;
  summary: string;
  recommendedFor: string;
  outputModes: string[];
  defaultName: string;
  defaultDescription: string;
  defaultTags: string[];
  defaultStarterPrompt: string;
  prompt: string;
};

export type SkillRuntimeGuide = {
  title: string;
  description?: string;
  items: string[];
};

export const SKILL_TEMPLATES: SkillTemplate[] = [
  {
    id: "basic-evidence-scan",
    label: "资料速读",
    summary: "先查历史结论，再检索已接入资料，给出证据驱动的简明回答。",
    recommendedFor: "明确主题、需要快速从上传文档或资料源中提炼结论",
    outputModes: ["文本", "Markdown", "表格"],
    defaultName: "basic-evidence-scan",
    defaultDescription: "先查历史结论，再检索已接入资料，输出基于证据的简明总结。",
    defaultTags: ["资料检索", "总结", "证据"],
    defaultStarterPrompt: "请围绕当前主题先检索证据，再输出一份基于证据的简明总结。",
    prompt: `# 何时使用

当用户希望围绕一个明确主题，从已接入资料中快速查证、阅读和总结时使用。

如果用户明确要求长链推演、持续校准偏题、反复重规划，改用更强的深度研究类技能。

# 输入约束

- 主题优先来自当前用户请求。
- 如果调用技能时带了补充说明，只把它当补充范围，不当模板变量。
- 如果用户限定了资料来源、文件名、网页、ES 索引或数据库表，优先遵守。
- 未接入的资料来源不要假设可用。

# 工作步骤

1. 先调用 \`MemorySearch\` 查询当前主题是否已有稳定结论或术语口径。
2. 优先使用 \`SourceSearch\` 检索上传文档，必要时用 \`SourceRead\` 读取原文片段。
3. 如果当前资料库接入了 ES，再调用 \`EsSearch\` 获取候选证据。
4. 如果问题明确指向网页或数据库，分别使用 \`WebFetch\` 或 \`DbQuery\` 读取受控来源。
5. 只基于实际命中的证据总结，不补齐未命中的事实。
6. 输出前用 \`TopicDriftCheck\` 检查总结是否仍围绕主题。
7. 如果结论稳定且可复用，调用 \`MemoryWrite\` 写入一条高价值结论。

# 输出要求

- 默认先给用户一段简洁结论。
- 如果有多条证据来源，补一个 Markdown 列表说明来源和结论映射。
- 只有在比较关系明显时再调用 \`ArtifactEmit\` 输出表格。

# 记忆策略

- 只写稳定结论，不写中间推测。
- 默认写入 \`workspace\`。
- 线程内短期提醒可写入 \`thread\`。

# 禁止事项

- 不要展示原始工具返回、后台路径或内部调试信息。
- 不要把未命中的内容写成事实。
- 不要为了凑完整而扩写无证据结论。`,
  },
  {
    id: "deep-synthesis",
    label: "深度研究",
    summary: "围绕主题持续检索、校准偏题、递进综合，并在必要时重建分析路径。",
    recommendedFor: "多轮研究、跨资料综合、需要持续防偏题的问题",
    outputModes: ["Markdown", "表格", "关系图"],
    defaultName: "deep-synthesis",
    defaultDescription: "围绕主题持续检索、校准偏题、递进综合，并在必要时重建分析路径。",
    defaultTags: ["深度研究", "综合分析", "防偏题"],
    defaultStarterPrompt: "请围绕当前主题做深度研究，持续检查是否偏题，并在证据充分后给出综合结论。",
    prompt: `# 何时使用

当用户要做深度研究、跨上传文档 / 网页 / ES / 数据库证据综合，并且要求持续判断是否偏离主题时使用。

# 输入约束

- 当前主题来自用户最新意图。
- 如果用户发生打断、改题、重规划，旧路线立即失效。
- 调用技能时带入的补充说明只作为额外上下文，不进行占位符替换。

# 工作步骤

1. 先用 \`MemorySearch\` 查看历史上是否已经有相近主题的稳定结论、边界定义或术语解释。
2. 根据主题拆出 2 到 4 个最小研究子问题，不向用户暴露全部内部拆解细节。
3. 对每个子问题优先检索已接入资料：上传文档用 \`SourceSearch\` / \`SourceRead\`，ES 用 \`EsSearch\`，网页用 \`WebFetch\`，数据库用 \`DbQuery\`。
4. 只有当当前资料库允许受控文件读取时，才使用 \`glob_search\`、\`grep_search\`、\`read_file\` 补证。
5. 每得到一轮阶段性判断，都调用 \`TopicDriftCheck\` 检查候选结论是否偏题。
6. 如果结果为 \`off_topic\`，立刻收缩范围，放弃偏离路径。
7. 如果证据之间冲突，明确列出冲突点、缺口和下一步最小补证动作。
8. 在证据充分后形成综合结论，并区分“已证实”“高概率”“待验证”。
9. 把稳定的主题边界、术语口径和高价值结论写入 \`MemoryWrite\`。

# 输出要求

- 主回答先给综合结论，再给证据组织方式。
- 若证据链较长，优先调用 \`ArtifactEmit\` 输出 Markdown 或表格。
- 如存在实体关系、依赖链或影响链，输出 \`graph\` artifact。

# 记忆策略

- \`workspace\`：当前项目长期有价值的主题边界、术语、结论。
- \`tenant\`：跨用户仍成立的共享规则。
- \`thread\`：仅用于本轮临时假设或短期待办。

# 偏题控制

- 任何阶段性总结都不能跳过 \`TopicDriftCheck\`。
- 如果用户要求改题，按新题目重新组织子问题，不沿用旧结论。

# 禁止事项

- 不要把内部推演步骤完整暴露给最终用户。
- 不要把所有子任务平铺在消息列表。
- 不要把未证实假设写入 \`workspace\` 或 \`tenant\` 记忆。`,
  },
  {
    id: "expert-brainstorm",
    label: "专家头脑风暴",
    summary: "围绕重大议题组织多位专家视角的独立评估、交叉辩论、共识分歧整理和行动建议。",
    recommendedFor: "投资决策、政策判断、战略选择、复杂风险评估等需要多视角推演的问题",
    outputModes: ["Markdown", "表格", "关系图"],
    defaultName: "expert-brainstorm",
    defaultDescription: "围绕重大议题组织多位专家视角独立评估、交叉辩论、共识分歧地图与行动建议。",
    defaultTags: ["头脑风暴", "多视角", "决策分析", "共识地图"],
    defaultStarterPrompt:
      "请围绕当前议题组织一场多专家头脑风暴，先做议题澄清，再给出独立评估、交叉辩论、共识分歧地图和可执行建议。",
    prompt: `# 何时使用

当用户要求从多位专家、多个学派或多个战略视角审视同一议题，并最终落到可执行决策建议时使用。

适合：

- 投资买卖时机判断
- 地缘政治、产业、政策与风险决策
- 需要同时比较乐观、审慎、冲突、合作等不同框架的议题

如果用户只要单一路径的事实总结，优先用 \`basic-evidence-scan\` 或 \`deep-synthesis\`。

# 输入约束

- 先确认议题、时间锚点、评估窗口、决策目标和用户约束。
- 如果用户指定了专家、流派或分析框架，优先使用这些视角。
- 如果用户未指定，主动补成 3 到 7 个差异明确的视角，不要堆重复立场。
- 如果使用历史人物、已故学者或知名专家名义，必须把他们当作“理论视角”或“分析框架”，不要伪造原话、私下信息或现实背书。
- 所有判断都要区分“证据支持的事实”和“基于该视角的推演”。

# 工作步骤

1. 先调用 \`MemorySearch\`，查看当前议题是否已有稳定定义、关键变量、历史判断或风险边界。
2. 优先从已接入资料获取证据：上传文档用 \`SourceSearch\` / \`SourceRead\`，网页用 \`WebFetch\`，ES 用 \`EsSearch\`，数据库用 \`DbQuery\`。
3. 证据不足时，先明确缺口，再做有限度情景推演，不要把推演写成已证实事实。
4. 在正式进入多专家视角前，先输出一段议题澄清：用户到底在问什么决策，时间窗口是什么，哪些变量最关键。
5. 为每位专家或视角分别给出：核心判断、关键逻辑、最看重的信号、对决策的直接含义。
6. 每轮阶段性判断后调用 \`TopicDriftCheck\`，确保讨论没有滑向人物生平、理论介绍或无关旁枝。
7. 在独立评估之后，组织交叉辩论：只围绕最关键的 2 到 4 个冲突点辩，不要让所有角色彼此泛泛回应。
8. 汇总共识和分歧，并把真正影响决策的冲突点整理成矩阵或结构化列表。
9. 在最终建议中回答三个问题：现在该不该做、什么条件下继续做/暂停、哪些标志性事件会改变判断。
10. 对长期可复用的判断框架、风险触发器和术语口径调用 \`MemoryWrite\`，不要把整场角色辩论原样写入记忆。

# 输出要求

- 默认按下面结构输出：
  - Phase 0：议题构建
  - Phase 1：多位专家独立评估
  - Phase 2：交叉辩论
  - Phase 3：共识 / 分歧地图
  - Phase 4：综合分析报告与行动建议
- 如果内容较长，优先调用 \`ArtifactEmit\` 输出一份 \`markdown\` 报告，标题建议为 \`专家会诊 / 综合结论\`。
- 每位专家独立分析建议各输出一份 \`markdown\` 结果卡，标题建议为 \`专家视角 / 专家名\`。
- 对“共识 / 分歧地图”“风险矩阵”“买入 / 卖出触发条件”优先调用 \`ArtifactEmit\` 输出 \`table\`，标题建议为 \`专家会诊 / 共识与分歧\` 或 \`专家会诊 / 风险矩阵\`。
- 只有在确实需要表达“立场冲突链路”“因果链”或“联盟 / 对抗关系”时，再输出 \`graph\`。
- 每位专家视角都应回到用户问题本身，而不是展开成长篇理论综述。
- 最终建议必须给出触发条件、观察指标、风险提示和可能的反转信号。

# 记忆策略

- \`thread\`：当前讨论里的临时假设、未确认冲突点。
- \`workspace\`：对同类决策长期有价值的判断框架、风险清单、信号体系。
- \`tenant\`：跨用户仍成立的通用方法论、术语定义和监测口径。

# 偏题控制

- 如果讨论开始变成“介绍这位专家是谁”，立刻收回到“这个视角对当前决策意味着什么”。
- 如果用户要的是买卖建议、行动方案或政策选择，每一阶段都必须回到“如何决策”。
- 如果证据明显不足，直接标记为高不确定性情景，不要继续扩写成确定性结论。

# 禁止事项

- 不要伪造专家原话、访谈内容或现实表态。
- 不要把理论人物写成真的参加了当前会议。
- 不要把内部推演过程当成逐字思考记录暴露给用户。
- 不要只给观点，不给触发条件、反转信号和行动含义。
- 不要展示原始工具返回、后台路径或调试信息。`,
  },
  {
    id: "report-table-chart",
    label: "报告与图表",
    summary: "把检索和分析结果整理为文本、Markdown、表格、图表或关系图并稳定渲染到页面。",
    recommendedFor: "需要稳定 artifact 交付，而不只是自然语言回答的问题",
    outputModes: ["Markdown", "表格", "图表", "关系图"],
    defaultName: "report-table-chart",
    defaultDescription: "把检索和分析结果整理为文本、Markdown、表格、图表或关系图并稳定渲染到前端。",
    defaultTags: ["报告", "图表", "结构化结果"],
    defaultStarterPrompt: "请围绕当前主题检索资料，并把结果整理成适合页面渲染的 Markdown、表格、图表或关系图。",
    prompt: `# 何时使用

当用户明确要求结果以表格、图表、关系图或 Markdown 报告形式输出时使用。

# 输入约束

- 先确认用户要的是对比、趋势、分布、关系还是正文报告。
- 如果用户没指定，按信息结构自动选最合适的 artifact 类型。

# 工作步骤

1. 先通过 \`MemorySearch\` 查找已有口径，避免重复定义指标或字段。
2. 收集证据，按已接入来源选择 \`SourceSearch\` / \`SourceRead\`、\`EsSearch\`、\`WebFetch\` 或 \`DbQuery\`。
3. 先形成规范化数据结构，再决定是否调用 \`ArtifactEmit\`。
4. 输出前调用 \`TopicDriftCheck\`，避免图表或表格表达偏离主题。
5. 对稳定规则或指标口径调用 \`MemoryWrite\` 做长期沉淀。

# Artifact 选择规则

- 对比多个来源或条目：\`table\`
- 展示趋势、分布、占比：\`chart\`
- 展示实体关联、依赖、引用链：\`graph\`
- 输出长段说明和小节结构：\`markdown\`
- 只需一句结果：\`text\`

# 输出要求

- \`table\` payload 只放结构化列和行，不混入长段 prose。
- \`chart\` payload 使用稳定字段名，避免一列一义的临时命名。
- \`graph\` payload 中节点和边都要有稳定 \`id\` 或关系字段。
- 如果 artifact 已足够表达，不要再重复一遍同长度自然语言。

# 记忆策略

- 把稳定指标口径、分类定义和复用价值高的结论写入 \`workspace\` 或 \`tenant\`。
- 不把一次性渲染细节写进长期记忆。

# 禁止事项

- 不要把工具原始 JSON 原封不动展示给用户。
- 不要输出字段名混乱的图表 payload。
- 不要为了视觉效果伪造数据点。`,
  },
  {
    id: "multi-source-answer",
    label: "多来源问答",
    summary: "按用户问题自动选择上传文档、网页、ES 或数据库，只呈现必要证据和可追问结果。",
    recommendedFor: "资料来源较多、用户希望像聊天一样直接提问的场景",
    outputModes: ["文本", "Markdown", "表格"],
    defaultName: "multi-source-answer",
    defaultDescription: "根据问题自动选择上传文档、网页、ES 或数据库来源，并输出可追问的证据化回答。",
    defaultTags: ["多来源", "问答", "资料库"],
    defaultStarterPrompt: "请基于当前资料库回答我的问题，必要时自动选择上传文档、网页、ES 或数据库来源。",
    prompt: `# 何时使用

当用户以自然语言提问，但没有指定具体资料来源时使用。

# 输入约束

- 不要求用户理解资料来源的后台配置。
- 先根据问题判断最可能的来源类型，再选择工具。
- 如果资料不足，明确说明缺口，而不是让用户去理解后台错误。

# 工作步骤

1. 先调用 \`MemorySearch\` 查找是否已有稳定答案、术语口径或历史限定条件。
2. 如果问题像文档内容问答，优先用 \`SourceSearch\`，再用 \`SourceRead\` 读取关键片段。
3. 如果问题指向网页内容，使用已接入网页的 \`WebFetch\`。
4. 如果问题指向业务数据、统计或明细列表，使用 \`DbQuery\` 执行只读 SQL。
5. 如果问题指向搜索索引或日志文本，使用 \`EsSearch\`。
6. 每轮证据合并后调用 \`TopicDriftCheck\`，确认回答没有偏离用户问题。
7. 对可复用的术语解释、查询口径或稳定结论调用 \`MemoryWrite\`。

# 输出要求

- 先回答用户问题，再说明引用了哪些来源。
- 如果来源很多，只展示最关键的 3 到 5 条证据。
- 若结果适合对比，调用 \`ArtifactEmit\` 输出表格；若只是结论，保持简洁文本。

# 记忆策略

- 当前会话中的临时限定只写入 \`thread\`。
- 资料库长期稳定的术语、口径、结论写入 \`workspace\`。
- 团队通用规则才写入 \`tenant\`。

# 偏题控制

- 如果工具命中的资料与问题无关，停止扩展同一路径，换一个来源或向用户说明缺口。
- 用户打断或改题时，立即按新问题重新选择来源。

# 禁止事项

- 不要向用户展示原始工具返回、SQL 调试细节、后台路径或内部错误堆栈。
- 不要要求用户提供服务器目录。
- 不要把未接入来源说成已经检索过。`,
  },
];

export const WEB_SKILL_RUNTIME_GUIDE: SkillRuntimeGuide[] = [
  {
    title: "当前能力边界",
    description: "这些是当前 Web Agent 已稳定提供的能力，设计技能时应围绕它们组织流程。",
    items: [
      "检索上传文档、读取关键片段",
      "读取受控网页、检索 ES、查询只读数据库",
      "写入和读取会话 / 资料集 / 团队记忆",
      "检查是否偏题，并把结果输出成文本、表格、图表或关系图",
    ],
  },
  {
    title: "作用范围",
    description: "技能和记忆都需要说明适用范围，避免多人协作时语义混乱。",
    items: [
      "技能可保存为资料集级或团队级",
      "记忆可写入当前会话、资料集或团队",
      "同名时会优先使用更贴近当前资料集的技能",
    ],
  },
  {
    title: "推荐写法",
    description: "一份稳定可复用的 Web Agent 技能，最好默认满足这些结构要求。",
    items: [
      "先写何时使用，再写输入约束和工作步骤",
      "显式说明什么时候检查是否偏题",
      "显式说明什么时候输出表格、图表或关系图",
      "区分当前会话 / 资料集 / 团队的记忆写入策略",
      "正文只写流程要求，不暴露内部调试信息",
    ],
  },
  {
    title: "不要假设",
    description: "这些能力当前没有稳定承诺，直接写进技能会让运行结果不稳定。",
    items: [
      "不要假设可以任意改写本地文件",
      "不要把 CLI 式路径和全局搜索约定暴露给用户",
      "附加参数当前不会做模板变量替换",
    ],
  },
];
